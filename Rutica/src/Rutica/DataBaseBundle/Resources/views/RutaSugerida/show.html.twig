{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RutaSugerida</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Distancia</th>
                <td>{{ entity.distancia }}</td>
            </tr>
            <tr>
                <th>Preciototal</th>
                <td>{{ entity.precioTotal }}</td>
            </tr>
            <tr>
                <th>Tiempo</th>
                <td>{{ entity.tiempo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('rutasugerida') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('rutasugerida_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RutaSugerida list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Distancia</th>
                <th>Preciototal</th>
                <th>Tiempo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('rutasugerida_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.distancia }}</td>
                <td>{{ entity.precioTotal }}</td>
                <td>{{ entity.tiempo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('rutasugerida_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rutasugerida_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('rutasugerida_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
